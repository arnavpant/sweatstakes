---
quick: 260414-82g
type: feat
tags: [dashboard, ui, dark-mode, design-tokens, royal-navy, emerald]
commits:
  - 736a02b — refactor(dashboard): extract UserAvatar, delete StreakCounter
  - 7aeb7c8 — feat(dashboard): redesign Dashboard with dark-navy + emerald visual style
---

# Quick 260414-82g — Dashboard redesign (dark-navy + emerald)

## What changed

Visual + structural redesign of the Dashboard page only. Inspired by
user-supplied component code; locked decisions captured in chat:

- Page background flipped to deep navy (`--color-background` token resolves to `#001233`).
- Card backgrounds use the project's existing surface-container ladder (lighter navy shades) — no new tokens added.
- All on-card text mapped to `--color-on-surface` / `--color-on-surface-variant` (light) for contrast.
- Emerald (`--color-secondary`) used for accent borders + the streak section's positive states.
- Orange streak pill kept as-is (intentional visual variety) — `bg-orange-100` + `text-orange-500/600`.
- Plus Jakarta Sans (already wired) — no font change.

## Structural changes

- New shared `src/components/dashboard/user-avatar.tsx` (avatar with ring + initials fallback).
- `streak-counter.tsx` deleted — its UI is absorbed into `DayDots` (single card with day-dot row + orange streak pill in the top-right).
- `member-card.tsx` deleted — `MemberCardRow` now inlines a tiny `MiniDayDots` directly.
- `MemberCardRow` filters out the current user (drops the pinned-first sort) and returns `null` when the row is empty.
- `MemberAvatarRow` (lives in `src/components/connections/`, but ONLY consumed by Dashboard per grep) now has a header row: `material-symbols-outlined` "group" icon + "Challenge Members" label.
- `PhotoGallery` re-framed in the new dark card with emerald accent border + "Recent Check-ins" header + `hover:scale-105` transition.
- `dashboard/page.tsx` rewritten to: `min-h-screen bg-background` + `max-w-md mx-auto` mobile-centered shell, 2-line greeting (`Welcome back,` / first name bold), inline `EmptyState` for the no-challenge view.

## Critical correctness note

`checkedInDays` is **ISO date strings** (e.g. `"2026-04-13"`), not day-name strings.
The inspo's `checkedInDays.includes("Mon")` was illustrative only. Both `DayDots`
and `MiniDayDots` use `addDays(weekStart, i)` over Mon–Sun and a `Set` lookup
to render the colored dots, preserving the project's real data shape.

## Auto-fixes (executor caught while running)

1. `src/components/streaks/streak-section.tsx` (separate `/streaks` page) imported the
   deleted `StreakCounter`. The equivalent fire-emoji rendering was inlined directly
   into `StreakSection` so the `/streaks` page still works.
2. Three pre-existing tests asserted on copy that the redesign changed
   (`Welcome,` → `Welcome back`, `Challenge active` → `Challenge Members`,
   `No active challenge yet.` → period dropped) — assertions updated to match new copy.

## Verification

- `npx tsc --noEmit` — clean
- `npx vitest run --reporter=dot` — **281/281 passing** (delta −1 from baseline 282 because the StreakCounter test was deleted with the component)
- Greps clean: `royal-blue`, `streak-counter`, `member-card['"]` → 0 hits in `src/`

## Files

- Created: `src/components/dashboard/user-avatar.tsx`
- Modified: `src/app/(protected)/dashboard/page.tsx`, `src/components/dashboard/day-dots.tsx`, `src/components/dashboard/member-card-row.tsx`, `src/components/dashboard/photo-gallery.tsx`, `src/components/connections/member-avatar-row.tsx`, `src/components/streaks/streak-section.tsx`, `tests/check-ins.test.ts`, `tests/auth.test.ts`, `tests/connections.test.ts`
- Deleted: `src/components/dashboard/streak-counter.tsx`, `src/components/dashboard/member-card.tsx`

## Out of scope (untouched)

Feed, Settings, Check-in, Login, auth, server actions, DB queries, planning artifacts.
