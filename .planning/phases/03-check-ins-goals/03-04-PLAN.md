---
phase: 03-check-ins-goals
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/components/layout/bottom-nav.tsx
  - tests/check-ins.test.ts
autonomous: false
requirements: [CHKN-01, CHKN-02, CHKN-03, CHKN-04, CHKN-05]

must_haves:
  truths:
    - "Bottom nav has a center FAB button that navigates to the check-in page"
    - "Bottom nav order is: Dashboard, Feed, [FAB], Streaks, Settings"
    - "FAB button is visually distinct -- larger, elevated, emerald green"
    - "Database schema is pushed to Supabase and tables exist"
    - "Full check-in flow works end-to-end: tap FAB -> camera -> capture -> preview -> submit -> appears in DB"
  artifacts:
    - path: "src/components/layout/bottom-nav.tsx"
      provides: "Updated bottom nav with center FAB for check-in"
      contains: "check-in"
  key_links:
    - from: "src/components/layout/bottom-nav.tsx"
      to: "src/app/(protected)/check-in/page.tsx"
      via: "Link or router.push to /check-in"
      pattern: "/check-in"
---

<objective>
Update the bottom navigation with a center FAB button for quick check-in access, push the database schema to Supabase, and verify the complete end-to-end flow.

Purpose: The FAB makes check-ins feel like the primary action of the app -- always one tap away from any screen. The schema push creates the actual database tables. The human verification checkpoint confirms the entire Phase 3 system works together.

Output: Updated bottom-nav with FAB, schema pushed to Supabase, and confirmed end-to-end functionality.
</objective>

<execution_context>
@C:/Users/Arnav/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Arnav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-check-ins-goals/03-CONTEXT.md
@.planning/phases/03-check-ins-goals/03-01-SUMMARY.md
@.planning/phases/03-check-ins-goals/03-02-SUMMARY.md
@.planning/phases/03-check-ins-goals/03-03-SUMMARY.md

<interfaces>
<!-- Bottom nav to be modified -->

From src/components/layout/bottom-nav.tsx (EXISTING):
```typescript
'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'

const tabs = [
  { label: 'Home', icon: 'home', href: '/dashboard' },
  { label: 'Streaks', icon: 'local_fire_department', href: '/streaks' },
  { label: 'Feed', icon: 'explore', href: '/feed' },
  { label: 'Settings', icon: 'settings', href: '/settings' },
]

export function BottomNav() {
  const pathname = usePathname()
  // ... renders tabs in a floating rounded pill nav bar
}
```

From src/app/(protected)/check-in/page.tsx (created by Plan 02):
```typescript
import { CameraView } from '@/components/check-in/camera-view'
export default function CheckInPage() {
  return <CameraView />
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bottom nav with center FAB and add final tests</name>
  <files>src/components/layout/bottom-nav.tsx, tests/check-ins.test.ts</files>
  <read_first>
    - src/components/layout/bottom-nav.tsx
    - src/app/globals.css
    - .planning/phases/03-check-ins-goals/03-CONTEXT.md (D-19, D-20)
  </read_first>
  <action>
    **1. Update `src/components/layout/bottom-nav.tsx`**

    Per D-19: Check-in button is a center FAB on the bottom nav bar -- always accessible, like Instagram's + button.
    Per D-20: The FAB replaces the middle nav item. Bottom nav becomes: Dashboard, Feed, [FAB], Streaks, Settings.

    Modify the `tabs` array and rendering logic:

    Replace the current 4-tab array with a 5-slot layout:
    ```typescript
    const tabs = [
      { label: 'Home', icon: 'home', href: '/dashboard' },
      { label: 'Feed', icon: 'explore', href: '/feed' },
      // FAB goes here (rendered separately, not as a tab)
      { label: 'Streaks', icon: 'local_fire_department', href: '/streaks' },
      { label: 'Settings', icon: 'settings', href: '/settings' },
    ]
    ```

    In the JSX, split the rendering into three parts:
    a. First two tabs (Dashboard, Feed) -- rendered as Links, same styling as current.
    b. Center FAB -- a Link to `/check-in` with distinct styling:
       - Larger: `w-14 h-14` (56px).
       - Elevated: negative margin top to "pop out" of the nav bar: `-mt-5` or `relative -top-3`.
       - Emerald green background: `bg-secondary`.
       - Rounded full circle: `rounded-full`.
       - White icon inside: `text-on-secondary` or white. Use Material Symbol `add_a_photo` or `photo_camera` icon.
       - Shadow for elevation: `shadow-lg shadow-secondary/30`.
       - No label text underneath (just the icon).
       - Add `aria-label="Check in"` for accessibility.
    c. Last two tabs (Streaks, Settings) -- rendered as Links, same styling as current.

    The FAB should NOT have the active state styling (gradient background) since it's always a call-to-action button, not a navigation destination that shows as "active".

    Hide the bottom nav when the user is on the `/check-in` page (since CameraView is full-screen):
    ```typescript
    const pathname = usePathname()
    if (pathname === '/check-in') return null
    ```

    **2. Add final integration tests to `tests/check-ins.test.ts`**

    Add describe block "Bottom Nav FAB (CHKN-01)":
    - bottom-nav.tsx contains /check-in href
    - bottom-nav.tsx contains aria-label for check-in button
    - bottom-nav.tsx has 4 regular tabs plus FAB center element
    - bottom-nav.tsx hides on /check-in route (contains check for pathname)

    Add describe block "End-to-End Integration (all CHKN)":
    - All required source files exist (schema, actions, week utils, camera components, dashboard components, settings components, bottom nav, check-in page)
    - submitCheckInAction and updateWeeklyGoalAction are exported from check-ins actions
    - getWeeklyProgress and computeStreak are exported from week utils
    - Dashboard imports both DayDots and StreakCounter
    - Settings imports GoalStepper
    - Check-in page imports CameraView
    - Bottom nav links to /check-in
  </action>
  <verify>
    <automated>npx vitest run tests/check-ins.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/check-in" src/components/layout/bottom-nav.tsx` returns a match
    - `grep "bg-secondary" src/components/layout/bottom-nav.tsx` returns a match (FAB emerald background)
    - `grep -c "Home\|Feed\|Streaks\|Settings" src/components/layout/bottom-nav.tsx` returns 4 (four regular tabs)
    - `grep "check-in" src/components/layout/bottom-nav.tsx` returns a match for hiding logic or href
    - `npx vitest run tests/check-ins.test.ts` passes
    - `npx vitest run` (full suite) passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>
    Bottom nav has 5 slots: Dashboard, Feed, [FAB], Streaks, Settings. FAB is emerald green circle with camera icon linking to /check-in. Nav hides on check-in page. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push database schema to Supabase</name>
  <files></files>
  <read_first>
    - src/db/schema.ts
    - drizzle.config.ts
    - package.json
  </read_first>
  <action>
    Run `npx drizzle-kit push` to push the schema changes to the Supabase database.

    This creates:
    - `check_ins` table with columns: id, user_id, challenge_id, photo_url, checked_in_date, created_at
    - Adds `weekly_goal` column to `challenge_members` table (smallint, default 3)
    - Creates indexes: check_ins_user_date_idx, check_ins_challenge_idx

    If the push command prompts for confirmation of destructive changes (e.g., renaming columns), answer carefully. The expected changes are additive only:
    - New table: check_ins (CREATE TABLE)
    - New column: challenge_members.weekly_goal (ALTER TABLE ADD COLUMN)
    - New indexes (CREATE INDEX)

    No existing columns should be dropped or renamed.

    If `DATABASE_URL` is not configured or the connection fails:
    1. Check `.env.local` exists and has DATABASE_URL
    2. The URL should be the Supabase Transaction mode connection string (port 6543)
    3. Get it from: Supabase Dashboard > Project Settings > Database > Connection string > Transaction mode (use connection pooler)
    4. If connection still fails, mark this task for human intervention

    After successful push, verify by running `npx drizzle-kit push` again -- it should report no changes needed.
  </action>
  <verify>
    <automated>npx drizzle-kit push 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` completes without error
    - Running `npx drizzle-kit push` a second time reports no pending changes
    - `npm run build` still succeeds after push
  </acceptance_criteria>
  <done>
    Database schema is synchronized with Drizzle schema. check_ins table exists in Supabase PostgreSQL. challenge_members table has weekly_goal column. All indexes created.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 3 check-in and goals system</name>
  <files></files>
  <action>
    Human verification of the complete Phase 3 system. All automated work is complete -- this checkpoint confirms visual and functional correctness.

    Complete Phase 3 check-in and goals system:
    1. Database schema with check_ins table and weeklyGoal column on challenge_members
    2. BeReal-style dual-camera check-in flow (rear + selfie with 3s countdown + composite preview)
    3. Photo compression and upload to Supabase Storage
    4. Server Actions for submitting check-ins and updating weekly goals
    5. Goal stepper on Settings page (1-7 days per week)
    6. Day dots progress tracker on Dashboard (M T W T F S S)
    7. Streak counter on Dashboard (fire emoji + week count)
    8. Center FAB on bottom nav for quick check-in access

    **Prerequisites:**
    - Supabase Storage bucket "check-in-photos" must be created with RLS policies (see user_setup in Plan 02)
    - Database schema must be pushed (Task 2 above)
    - You must be logged in and in a challenge

    **Test the check-in flow:**
    1. Open the app on your phone browser (or localhost on desktop)
    2. Verify the bottom nav now shows: Home, Feed, [green FAB], Streaks, Settings
    3. Tap the green FAB button
    4. Camera should open full-screen with rear camera active
    5. Tap the shutter button to capture the rear photo
    6. Camera should auto-flip to front camera with 3... 2... 1... countdown
    7. After countdown, selfie auto-captures
    8. Preview should show: large rear photo with small selfie thumbnail in top-left corner
    9. Tap "Retake" -- should restart entire flow
    10. Capture both photos again, then tap "Submit"
    11. Should see loading state, then navigate back to dashboard

    **Test goal setting:**
    12. Go to Settings
    13. Verify "Weekly Goal" section with stepper (minus, number, plus)
    14. Try changing the goal with plus/minus buttons
    15. Verify the number updates and range is clamped to 1-7

    **Test progress display:**
    16. Go to Dashboard
    17. Verify day dots row shows M T W T F S S
    18. If you just checked in, today's dot should be filled (emerald green)
    19. Verify progress count shows like "1/3 days"
    20. Verify streak counter appears below day dots (fire emoji if streak > 0, or nothing/muted text)
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    User has confirmed: camera flow works end-to-end, goal stepper updates correctly, day dots and streak counter display on dashboard. Phase 3 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bottom nav -> check-in page | Navigation from any screen to camera; camera requires secure context |
| Schema push -> Supabase DB | Schema changes applied to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | bottom-nav.tsx | accept | Navigation links are client-side routing; no security boundary crossed. The check-in page is under (protected) layout requiring auth. |
| T-03-15 | Denial of Service | schema push | accept | One-time operation; no ongoing risk. Schema is additive only (no drops). |
</threat_model>

<verification>
- `npx vitest run tests/check-ins.test.ts` passes
- `npx vitest run` (full suite) passes
- `npm run build` succeeds
- Bottom nav has FAB linking to /check-in
- Schema pushed to Supabase (check_ins table + weekly_goal column exist)
- Human verification confirms full end-to-end flow
</verification>

<success_criteria>
- FAB button on bottom nav always accessible from any screen (D-19)
- Bottom nav order: Dashboard, Feed, [FAB], Streaks, Settings (D-20)
- Schema pushed and tables exist in Supabase
- End-to-end flow confirmed by human testing
</success_criteria>

<output>
After completion, create `.planning/phases/03-check-ins-goals/03-04-SUMMARY.md`
</output>
