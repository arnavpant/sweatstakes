---
quick: 260414-7f4
type: fix
tags: [tests, vitest, connections, string-match]
commit: a76267c
---

# Quick 260414-7f4 — Repair fragile string-match in connections leave test

## What changed

`tests/connections.test.ts:189` — swapped the literal substring assertion
`expect(content).toContain('db.delete(challengeMembers)')` for a regex
`expect(content).toMatch(/db\s*\.delete\(\s*challengeMembers\s*\)/)`. The
production `leaveChallengeAction` uses a multi-line drizzle builder
(`db\n  .delete(challengeMembers)\n  .where(...)`), so the literal substring
never appeared and the test had been failing the whole session.

Also staged the new dev-tool script `scripts/wipe-test-points.mjs` (clears
`point_transactions` rows for testing — used this session to wipe the
leader-callout test data before final cleanup) into the same commit since
it's adjacent dev-tooling housekeeping.

## Files

- `tests/connections.test.ts` — assertion swap (line 189)
- `scripts/wipe-test-points.mjs` — new dev script, ~31 lines

`src/lib/actions/connections.ts` is unchanged — implementation was already correct.

## Verification

`npx vitest run --reporter=dot` → **282 passed (282)**, 12 test files.
Up from 281/282 (the one remaining failure was this exact test).
