---
phase: quick-260414-6lk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/leader-callout.tsx
  - src/lib/utils/leader-callout.ts
  - tests/leader-callout.test.ts
  - src/app/(protected)/dashboard/page.tsx
  - src/lib/queries/dashboard.ts
  - tests/dashboard-queries.test.ts
autonomous: true
requirements:
  - QUICK-260414-6lk
must_haves:
  truths:
    - "Dashboard page renders without the leader callout section"
    - "No code references LeaderCallout, LeaderCalloutInput, computeLeaderCallout, or getBalancesForChallenge"
    - "TypeScript compiles with no new errors"
    - "All remaining tests pass"
    - "getMemberProgressRows and getRecentCheckInPhotos still work unchanged"
  artifacts:
    - path: "src/components/dashboard/leader-callout.tsx"
      provides: "MUST NOT EXIST — deleted"
    - path: "src/lib/utils/leader-callout.ts"
      provides: "MUST NOT EXIST — deleted"
    - path: "tests/leader-callout.test.ts"
      provides: "MUST NOT EXIST — deleted"
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard page, leader-callout imports/state/JSX removed"
      contains: "MemberAvatarRow, DayDots, StreakCounter, MemberCardRow, PhotoGallery"
    - path: "src/lib/queries/dashboard.ts"
      provides: "getMemberProgressRows + getRecentCheckInPhotos only"
      exports: ["getMemberProgressRows", "getRecentCheckInPhotos"]
    - path: "tests/dashboard-queries.test.ts"
      provides: "Tests for getRecentCheckInPhotos only"
  key_links:
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/lib/queries/dashboard.ts"
      via: "named imports"
      pattern: "getMemberProgressRows, getRecentCheckInPhotos"
---

<objective>
Remove the dashboard "leader callout" feature in full — the callout UI, the pure
utility that generates its copy, its tests, and the now-orphan
`getBalancesForChallenge` query helper and its tests. Keep every other
dashboard section (avatar row, day dots, streak counter, member card row, photo
gallery) intact.

Purpose: User feedback — the leader callout is not useful. Deleting dead code
keeps the dashboard focused and prevents drift.

Output: Three files deleted, three files edited, atomic commit, no TS/test
regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Exports that MUST REMAIN in src/lib/queries/dashboard.ts after this plan -->
```typescript
export async function getRecentCheckInPhotos(challengeId: string, limit = 6)
export async function getMemberProgressRows(challengeId: string)
```

<!-- The leader callout JSX block being dropped from dashboard/page.tsx -->
```tsx
<LeaderCallout balances={balances} viewerUserId={user.id} />
```

<!-- Import block being pruned in dashboard/page.tsx -->
```ts
// REMOVE: import { LeaderCallout } from '@/components/dashboard/leader-callout'
// REMOVE: import type { LeaderCalloutInput } from '@/lib/utils/leader-callout'
// EDIT: drop `getBalancesForChallenge,` from the '@/lib/queries/dashboard' import
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete leader-callout files and prune all consumers</name>
  <files>
    src/components/dashboard/leader-callout.tsx (DELETE via git rm),
    src/lib/utils/leader-callout.ts (DELETE via git rm),
    tests/leader-callout.test.ts (DELETE via git rm),
    src/app/(protected)/dashboard/page.tsx (EDIT),
    src/lib/queries/dashboard.ts (EDIT),
    tests/dashboard-queries.test.ts (EDIT)
  </files>
  <action>
Perform all six mutations in a single atomic change so the project compiles and
tests pass at every committed state. Execute in this order:

**Step 1 — `git rm` the three dead files** (run from repo root):
```bash
git rm src/components/dashboard/leader-callout.tsx
git rm src/lib/utils/leader-callout.ts
git rm tests/leader-callout.test.ts
```

**Step 2 — Edit `src/app/(protected)/dashboard/page.tsx`** (exact surgical changes;
do NOT reformat or touch unrelated code):

- Delete line 9 entirely:
  `import { LeaderCallout } from '@/components/dashboard/leader-callout'`
- Delete line 18 entirely:
  `import type { LeaderCalloutInput } from '@/lib/utils/leader-callout'`
- In the `import { ... } from '@/lib/queries/dashboard'` block (lines 13-17),
  remove the `getBalancesForChallenge,` line so only `getMemberProgressRows`
  and `getRecentCheckInPhotos` remain imported.
- Delete line 66 entirely:
  `let balances: LeaderCalloutInput[] = []`
- Delete lines 80-81 entirely (the comment + assignment):
  ```
  // DASH-01: balance aggregation for leader callout
  balances = await getBalancesForChallenge(challengeId)
  ```
- Delete line 140 entirely from the JSX:
  `<LeaderCallout balances={balances} viewerUserId={user.id} />`

Leave everything else in that file untouched (MemberAvatarRow, DayDots,
StreakCounter, MemberCardRow, PhotoGallery, empty state, greeting, membership
query, weeklyProgress/streak computation, memberRows computation, recentPhotos
computation).

**Step 3 — Edit `src/lib/queries/dashboard.ts`:**

- Delete the `getBalancesForChallenge` exported function and its preceding
  JSDoc block (lines 13-35 in the current file — the doc comment that starts
  with `/**` above the function through the closing `}` of the function).
- Since `getBalancesForChallenge` was the only consumer of `pointTransactions`,
  `and`, and `sql`, prune those from the imports:
  - From `import { challengeMembers, checkIns, pointTransactions } from '@/db/schema'`
    remove `pointTransactions` → leaves `{ challengeMembers, checkIns }`.
  - From `import { eq, and, sql, desc } from 'drizzle-orm'` remove `and` and
    `sql` → leaves `{ eq, desc }`.
- Keep `import 'server-only'`, keep `getRecentCheckInPhotos` and
  `getMemberProgressRows` exactly as-is.
- Update the file-header block comment if it enumerates the removed function
  (current header just says "Dashboard server query helpers" — no name change
  needed, but verify before saving).

**Step 4 — Edit `tests/dashboard-queries.test.ts`:**

- Delete the entire `describe('getBalancesForChallenge', ...)` block
  (lines 63-81 in the current file, including the closing `})`).
- `innerJoin` and `groupBy` properties on the mock `builder` object can remain
  — they're harmless no-ops and removing them risks over-editing. Leave them.
- Update the file-header JSDoc: the current comment says "captures the
  drizzle builder chain for getRecentCheckInPhotos and getBalancesForChallenge".
  Change it to read only "getRecentCheckInPhotos".
- Leave the remaining `describe('getRecentCheckInPhotos', ...)` block
  untouched.

**Out of scope — do NOT touch:**
- `.planning/phases/05-dashboard-feed-polish/` historical artifacts
- `point_transactions` schema
- Points server actions
- Any other dashboard section
- Existing DB test data
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
- All three files deleted from the working tree AND the git index (`git status`
  shows them as deleted/staged).
- `src/app/(protected)/dashboard/page.tsx` contains zero occurrences of
  `LeaderCallout`, `LeaderCalloutInput`, `getBalancesForChallenge`, or
  `balances` (verify with `grep -n "LeaderCallout\|getBalancesForChallenge\|LeaderCalloutInput\|balances" src/app/\(protected\)/dashboard/page.tsx` — should return nothing).
- `src/lib/queries/dashboard.ts` contains zero occurrences of
  `getBalancesForChallenge`, `pointTransactions`, `sql`, or `and` (verify with
  grep).
- `tests/dashboard-queries.test.ts` contains zero occurrences of
  `getBalancesForChallenge`.
- `npx tsc --noEmit` exits 0 with no new errors.
- `npm test` (or whichever test command `package.json` defines) passes all
  remaining tests. Test count drops by exactly the leader-callout suite + the
  two `getBalancesForChallenge` cases.
  </done>
</task>

</tasks>

<verification>
Run from repo root after the task completes:

1. `git status` shows the three deleted files staged and the three edited files
   modified.
2. `npx tsc --noEmit` — no errors.
3. `npm test` (check `package.json` scripts for exact name, likely `test` or
   `test:run`) — all remaining suites pass.
4. Grep check:
   ```
   grep -r "LeaderCallout\|getBalancesForChallenge\|leader-callout" src/ tests/
   ```
   Should return no matches.
5. Spot-check the dashboard renders: the page still has `MemberAvatarRow`,
   `DayDots`, `StreakCounter`, `MemberCardRow`, `PhotoGallery` in the JSX.
</verification>

<success_criteria>
- Three files deleted (`leader-callout.tsx`, `leader-callout.ts`,
  `leader-callout.test.ts`).
- Three files edited with only the surgical changes described above, no
  collateral formatting churn.
- `npx tsc --noEmit` passes.
- Remaining test suite passes.
- Single atomic commit covers all six file changes.
- Zero references to `LeaderCallout`, `computeLeaderCallout`,
  `LeaderCalloutInput`, `getBalancesForChallenge`, or the `leader-callout`
  path anywhere under `src/` or `tests/`.
</success_criteria>

<output>
After completion, create
`.planning/quick/260414-6lk-remove-leader-callout-dashboard-feature-/260414-6lk-SUMMARY.md`
summarizing files deleted, files edited, and test impact.
</output>
