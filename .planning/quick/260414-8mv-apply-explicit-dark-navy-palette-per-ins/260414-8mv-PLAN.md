---
quick_id: 260414-8mv
type: execute
autonomous: true
files_modified:
  - src/app/(protected)/dashboard/page.tsx
  - src/components/dashboard/day-dots.tsx
  - src/components/dashboard/member-card-row.tsx
  - src/components/dashboard/photo-gallery.tsx
  - src/components/dashboard/user-avatar.tsx
  - src/components/connections/member-avatar-row.tsx
must_haves:
  truths:
    - "Dashboard cards render with explicit dark-navy palette matching inspo"
    - "Text and accent colors match inspo exactly (no semantic-token indirection on these surfaces)"
    - "Streak pill renders unconditionally with gap-2 (not gap-1.5)"
    - "DayDots data flow (addDays + Set lookup) remains intact"
    - "All 281 existing tests pass; no type errors"
  artifacts:
    - path: "src/components/dashboard/day-dots.tsx"
      provides: "DayDots restyled with explicit navy palette"
    - path: "src/components/dashboard/member-card-row.tsx"
      provides: "MemberCardRow restyled with explicit navy palette"
    - path: "src/components/dashboard/photo-gallery.tsx"
      provides: "PhotoGallery restyled with explicit navy palette"
    - path: "src/components/dashboard/user-avatar.tsx"
      provides: "UserAvatar restyled with explicit navy palette"
    - path: "src/components/connections/member-avatar-row.tsx"
      provides: "MemberAvatarRow restyled with explicit navy palette"
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard page shell restyled with explicit navy palette"
  key_links:
    - from: "day-dots.tsx"
      to: "weekStart prop + checkedInDays"
      via: "addDays(weekStart, i) + new Set(checkedInDays).has(dateStr)"
      pattern: "addDays\\(weekStart"
---

<objective>
Apply the explicit dark-navy palette from the inspo treatment to the 6 dashboard surface files. This is a pure visual/className refactor — no JSX structure changes beyond two small fidelity fixes, no logic changes, no data-flow changes. Cards move from semantic tokens (`bg-surface-container`, `border-secondary/20`) to explicit inspo colors (e.g. `bg-[#0b1a3a]`, `border-white/10`), and text/accent classes align with the inspo treatment.

Purpose: Visual fidelity with the inspo mockup; pure styling change.
Output: 6 files with updated classNames; 281/281 tests still green; `tsc --noEmit` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/app/(protected)/dashboard/page.tsx
@src/components/dashboard/day-dots.tsx
@src/components/dashboard/member-card-row.tsx
@src/components/dashboard/photo-gallery.tsx
@src/components/dashboard/user-avatar.tsx
@src/components/connections/member-avatar-row.tsx

<structural_validation_notes>
Planner verified before writing this plan:
- All 6 files exist and contain the className strings referenced in the Color Mapping Table below.
- No JSX structure changes required — only className edits — EXCEPT the two Structural Fidelity fixes listed below.
- `addDays(weekStart, i)` + `new Set(checkedInDays).has(dateStr)` lookup data flow in `day-dots.tsx` and `member-card-row.tsx` must remain intact (do not touch the logic lines).
- Existing deviations from inspo that the executor MUST fix back:
  1. `day-dots.tsx:54` currently has `{streak > 0 && ( ... )}` wrapping the streak pill. Inspo shows the pill unconditionally — REMOVE the `{streak > 0 &&}` conditional so the pill always renders.
  2. `day-dots.tsx:55` currently uses `gap-1.5` on the streak pill flex container. Inspo uses `gap-2` — change `gap-1.5` → `gap-2`.
</structural_validation_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply explicit dark-navy palette to 6 dashboard files</name>
  <files>
    src/app/(protected)/dashboard/page.tsx,
    src/components/dashboard/day-dots.tsx,
    src/components/dashboard/member-card-row.tsx,
    src/components/dashboard/photo-gallery.tsx,
    src/components/dashboard/user-avatar.tsx,
    src/components/connections/member-avatar-row.tsx
  </files>
  <action>
Apply the inspo's explicit dark-navy palette by replacing className strings ONLY. Do not touch JSX structure, props, imports, or logic (with two exceptions noted in the Structural Fidelity section below).

---

## COLOR MAPPING TABLE

### Cards (container surfaces)

Replace every occurrence of the card frame className combo with the explicit inspo values. The pattern to find across all 6 files:

| Find (current)                                             | Replace (inspo)                          |
| ---------------------------------------------------------- | ---------------------------------------- |
| `bg-surface-container`                                     | `bg-[#0b1a3a]`                           |
| `bg-surface-container-high`                                | `bg-[#13254f]`                           |
| `bg-surface-container-highest`                             | `bg-[#1a2f63]`                           |
| `border-secondary/20`                                      | `border-white/10`                        |
| `bg-background` (on dashboard page root)                   | `bg-[#050b1f]`                           |

Card frames appear as the combo `bg-surface-container border border-secondary/20 rounded-xl` in:
- `day-dots.tsx` (outer card)
- `member-card-row.tsx` (outer card)
- `photo-gallery.tsx` (empty-state card AND populated card)
- `member-avatar-row.tsx` (outer card)

Inner surfaces (`bg-surface-container-high`) appear in:
- `day-dots.tsx` (the 7 day-dot circles when NOT checked)
- `member-card-row.tsx` (the per-member row bg `bg-surface-container-high`, AND the MiniDayDots unchecked circles)
- `photo-gallery.tsx` (the thumbnail tile bg `bg-surface-container-high`)
- `user-avatar.tsx` (fallback initials bg `bg-surface-container-high`)
- `member-avatar-row.tsx` (avatar borders `border-surface-container-high`, overflow pill bg `bg-surface-container-high`, fallback initials bg `bg-surface-container-high`)

`bg-surface-container-highest` appears in:
- `member-card-row.tsx` (fallback initials bg on per-member row)

### Text & Accents

| Find (current)                                             | Replace (inspo)                          |
| ---------------------------------------------------------- | ---------------------------------------- |
| `text-on-surface`                                          | `text-white`                             |
| `text-on-surface-variant`                                  | `text-slate-400`                         |
| `text-on-secondary`                                        | `text-white`                             |
| `text-secondary`                                           | `text-emerald-400`                       |
| `bg-secondary` (on day-dot circles when checked)           | `bg-emerald-500`                         |
| `ring-secondary/30` (on UserAvatar)                        | `ring-emerald-500/40`                    |
| `ring-secondary/50` (today-ring on day-dots)               | `ring-emerald-400/60`                    |

Streak pill specifics (day-dots.tsx, already uses orange literals — keep these exact):
- `bg-orange-100` → `bg-orange-500/15` (emit-over-dark treatment)
- `text-orange-500` → `text-orange-400`
- `text-orange-600` → `text-orange-300`

---

## STRUCTURAL FIDELITY DIRECTIVE

Beyond the pure className swaps above, fix these two deviations from the inspo back to match:

1. **`src/components/dashboard/day-dots.tsx`** — Remove the `{streak > 0 && ( ... )}` conditional around the streak pill (currently at line ~54). The inspo renders the pill unconditionally. Unwrap the JSX so the streak pill `<div className="flex items-center gap-2 bg-orange-500/15 px-3 py-1.5 rounded-full">...</div>` always renders. (When `streak === 0` it will just show `0w streak` — that matches the inspo.)

2. **`src/components/dashboard/day-dots.tsx`** — On that same streak pill container, change `gap-1.5` → `gap-2` to match the inspo spacing.

Do NOT change:
- Any JSX structure other than removing the `{streak > 0 &&}` wrapper in day-dots.tsx.
- The `addDays(weekStart, i)` call or `new Set(checkedInDays).has(dateStr)` lookup in day-dots.tsx or member-card-row.tsx (the data flow stays intact).
- Any imports, props, interfaces, or function signatures.
- Any other `.tsx`/`.ts` file in the repo outside the 6 listed.
- Tailwind config, globals.css, or theme tokens — we are hardcoding inspo values inline on these surfaces ONLY.

---

## EXECUTION ORDER (suggested)

1. `src/app/(protected)/dashboard/page.tsx` — swap `bg-background` → `bg-[#050b1f]`, and swap the no-challenge card's `bg-surface-container border-secondary/20` + text colors per the table.
2. `src/components/dashboard/user-avatar.tsx` — swap fallback bg + ring + text per table.
3. `src/components/connections/member-avatar-row.tsx` — swap card frame + avatar borders + overflow pill + fallback initials + icon + text per table.
4. `src/components/dashboard/day-dots.tsx` — swap card frame + day-dot checked/unchecked bgs + today ring + text + streak pill colors. ALSO: remove `{streak > 0 &&}` conditional and change `gap-1.5` → `gap-2`.
5. `src/components/dashboard/member-card-row.tsx` — swap outer card frame + per-member row bg + MiniDayDots checked/unchecked bgs + fallback initials bg + all text classes per table.
6. `src/components/dashboard/photo-gallery.tsx` — swap empty-state card + populated card + thumbnail tile bg + text per table.

After each file, eyeball that no occurrence of `bg-surface-container`, `bg-surface-container-high`, `bg-surface-container-highest`, `border-secondary/20`, `text-on-surface`, `text-on-surface-variant`, `text-on-secondary`, `text-secondary`, `bg-secondary`, `ring-secondary/30`, `ring-secondary/50`, or `bg-background` remains in the 6 edited files. (These classes may still exist elsewhere in the repo — do not touch those.)
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --reporter=dot</automated>
  </verify>
  <done>
- All 6 files updated with explicit inspo palette per the Color Mapping Table.
- `day-dots.tsx`: streak pill unconditional (no `{streak > 0 &&}`), pill uses `gap-2`.
- `npx tsc --noEmit` exits clean (no type errors).
- `npx vitest run --reporter=dot` shows 281/281 passing (pure visual change, no test impact expected).
- No occurrences of `bg-surface-container*`, `border-secondary/20`, `text-on-surface*`, `text-on-secondary`, `text-secondary`, `bg-secondary`, `ring-secondary/*`, or `bg-background` remain in any of the 6 edited files.
- Single atomic commit: `style(dashboard): apply explicit dark-navy palette per inspo treatment`.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors.
- `npx vitest run --reporter=dot` — 281/281 passing (no tests assert on these className literals; pure visual change).
- Grep check: `bg-surface-container`, `border-secondary/20`, `text-on-surface`, `text-secondary`, `bg-secondary`, `ring-secondary`, `bg-background` should no longer appear in the 6 edited files (may still appear elsewhere — do not touch).
</verification>

<success_criteria>
- 6 files updated with explicit inspo colors per table.
- Two Structural Fidelity fixes applied in `day-dots.tsx` (unconditional streak pill + `gap-2`).
- Data flow (`addDays` + `Set` lookup) untouched.
- `tsc --noEmit` clean, 281/281 tests passing.
- Single atomic commit landed with message `style(dashboard): apply explicit dark-navy palette per inspo treatment`.
</success_criteria>

<output>
After completion, no SUMMARY required (quick mode, single atomic styling change). The commit itself is the artifact.
</output>
